org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true
kotlin.code.style=official

# Publishing Configuration
# Set these in your ~/.gradle/gradle.properties or CI environment variables

# For Maven Central (OSSRH)
ossrhUsername=
ossrhPassword=
signingKeyId=
signingPassword=
signingKey=

# Alternatively, use environment variables:
# export OSSRH_USERNAME=your_username
# export OSSRH_PASSWORD=your_password
# export GPG_PRIVATE_KEY=your_gpg_private_key
# export GPG_PASSPHRASE=your_gpg_passphrase
